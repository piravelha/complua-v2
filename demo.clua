
framerate :: 60

function map(tbl, fn)
  new := {}
  for tbl do
    new[idx] = fn(it)
  end
  return new
end

function main()
  array :: {1, 2, 3}
  new :: map(array, function(x)
    return x * framerate
  end)
  for new do
    print(it)
  end
end

#eval main()


